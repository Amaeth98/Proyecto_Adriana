# Deploy 06 - next

Requiere que existan `adri/node:latest`, la API levantada y la red compartida `red_adriana`.

```bash
cd ~/Proyecto_Adriana/docker/adriana/proyectos/deploy-tienda-online/deploy-06-next
docker compose up -d --build
docker compose ps
```
